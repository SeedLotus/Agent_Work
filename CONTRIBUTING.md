# 贡献指南

欢迎将新的项目和改进提交到本仓库。

## 结构要求

- 所有项目应放在 `projects/` 目录下
- 根级文档应放在 `docs/`
- 通用工具、脚本放在 `tools/`
- 模板放在 `templates/`

## 新增项目流程

1. 在 `projects/` 下新增项目目录
2. 为新项目创建 `README.md`
3. 在 `docs/projects.yaml` 或根目录 `projects.yaml` 中添加项目信息
4. 提交前确认根目录及项目目录结构完整
